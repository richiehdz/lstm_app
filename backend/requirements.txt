Django==5.1.2
psycopg2-binary==2.9.9

celery[redis]==5.4.0
django-celery-results==2.5.1

# Para después, cuando implementemos la API/rest:
djangorestframework==3.15.2

# Para leer CSV de forma cómoda (ya pensando en el MVP)
pandas==2.2.3

# (Más adelante agregamos PyTorch / TensorFlow, para que el build inicial no pese 20GB)
openpyxl==3.1.5